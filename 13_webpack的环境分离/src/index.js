const foo = 'hello babel'

const bar = (info) => {
  console.log(info)
}

const p = new Promise((resolve, reject) => {})

bar(foo)

const app = document.getElementById('app')
app.innerHTML = 'hi'

const arr = [1, 2]
